# Stick-Pack Price Typography Verification

Reduced both offer-block totals from `text-xl sm:text-2xl` to `text-lg sm:text-xl`. Reduced the mobile sticky-bar total from `text-lg` to `text-base`. CTA typography and supporting per-bag and per-day text remain unchanged, preserving the established information hierarchy.

Desktop and 390-pixel mobile captures confirm the decimal prices fit more comfortably without wrapping, overlap, or loss of emphasis. All 30 automated tests pass, and the production build succeeds.

