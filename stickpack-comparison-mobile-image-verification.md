# Mobile BrewNectar Comparison Image Verification

The mobile BrewNectar comparison scene keeps its existing `-72px` top position while increasing from 100 × 160 pixels to 132 × 198 pixels. This preserves the approved top reach and extends the image box to two pixels below the 58-pixel product-header row, visually aligning the bottom of the transparent scene with the BrewNectar header area.

The change remains isolated behind `md:hidden`; the desktop editorial composition is unchanged. Mobile and desktop full-page captures confirm the table grid remains aligned and no new horizontal overflow appears.

All 36 automated tests pass, and the production build succeeds.

